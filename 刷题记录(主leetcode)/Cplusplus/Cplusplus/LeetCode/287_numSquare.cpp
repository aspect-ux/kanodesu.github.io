#include<iostream>
#include<cmath>
using namespace std;
class Solution {
public:
    int numSquares(int n) {
        int b = sqrt(n),temp = 0;
        if(b -sqrt(n) == 0) return 1;
        for(int i = n - 1; i >= 0; i--){
            temp++;
            while(temp){

            }
        } 
    }
};
int main(){


    system("pause:");
    return 0;
}