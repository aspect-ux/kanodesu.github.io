#include<iostream>
#include<vector>
using namespace std;
class Solution {
public: //双重循环，外层循环控制右移位数，内层控制右移的数，关键点在于找到最大的按位与为>0的元素个数
    int largestCombination(vector<int>& candidates) {
        
    }
};
int main(){

    system("pause:");
    return 0;
}