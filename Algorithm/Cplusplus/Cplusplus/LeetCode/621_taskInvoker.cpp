#include<iostream>
#include<vector>
#include<algorithm>
#include<queue>
#include<unordered_map>
using namespace std;
// class Solution {
// public:
//     int leastInterval(vector<char>& tasks, int n) {
//         priority_queue<char,vector<char>> qu;
//         unordered_map<char,int> hash;
//         vector<char> v;
//         int n = tasks.size(),ans = 0;
//         char start = tasks[0];
//         sort(tasks.begin(),tasks.end());
//         for(char c : tasks) hash[c] ++;
//         for(auto c : hash){
//             qu.push(c.first);
//         }
//         for(int i = 0; i < qu.size(); i ++){
//             hash[qu[i]] --;
//             if()
//         }
//     }
// };

int main(){
   
    system("pause:");
    return 0;
}